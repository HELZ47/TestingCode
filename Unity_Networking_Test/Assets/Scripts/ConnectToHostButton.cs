using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class ConnectToHostButton : MonoBehaviour {

	//Fields
	public int hostIndex;
	Button myButton;

	// Use this for initialization
	void Awake () {
		myButton = GetComponent<Button> ();
	}
	
	// Update is called once per frame
	void Update () {
//		if (button
	}
	
}
